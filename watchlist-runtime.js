(function(){'use strict';const KEY='aktpro_watchlist_v5',LEGACY_KEYS=['aktpro_watchlist_v4','aktpro_watchlist_v3','aktpro_watchlist_v2'];const enc=v=>btoa(unescape(encodeURIComponent(JSON.stringify(v)))),dec=v=>JSON.parse(decodeURIComponent(escape(atob(v))));const norm=v=>String(v??'').trim().replace(/^WKN\s*[:#-]?\s*/i,'').replace(/[^A-Z0-9]/gi,'').toUpperCase();const clean=v=>String(v??'').trim();const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));function normalizeList(v){if(!Array.isArray(v))return[];const seen=new Set();return v.map(x=>typeof x==='string'?{key:norm(x),title:''}:{key:norm(x?.key||x?.wkn),title:clean(x?.title||x?.name||'')}).filter(x=>x.key&&!seen.has(x.key)&&seen.add(x.key))}function load(){try{const c=localStorage.getItem(KEY);if(c)return normalizeList(JSON.parse(c));for(const k of LEGACY_KEYS){const r=localStorage.getItem(k);if(r){const m=normalizeList(JSON.parse(r));localStorage.setItem(KEY,JSON.stringify(m));return m}}}catch(_){}return[]}let list=load();function save(){list=normalizeList(list);try{localStorage.setItem(KEY,JSON.stringify(list))}catch(e){console.error(e)}render();}function record(el){if(!el)return null;const s=el._stock||{};let key=norm(s.wkn||el.dataset.wkn||el.dataset.watchKey||el.querySelector('[data-wkn]')?.dataset.wkn||el.querySelector('.top10-wkn')?.textContent||'');let title=clean(el.querySelector('.top10-name')?.textContent||el.querySelector('.summary-main b')?.textContent||s.name||el.dataset.watchTitle||'');return key?{key,title:title&&norm(title)!==key?title:''}:null}function toggle(r){if(!r?.key)return;const k=norm(r.key),i=list.findIndex(x=>x.key===k);if(i>=0)list.splice(i,1);else list.push({key:k,title:clean(r.title)});save()}function openDetail(k){k=norm(k);const hit=[...document.querySelectorAll('#individuals .stock-group')].find(g=>norm(g._stock?.wkn||g.dataset.wkn||g.dataset.watchKey)===k);if(hit){hit.open=true;hit.scrollIntoView({behavior:'smooth',block:'center'});return}const input=document.getElementById('wkn1'),btn=document.getElementById('analyzeBtn');if(!input||!btn)return;input.value=k;input.dispatchEvent(new Event('input',{bubbles:true}));setTimeout(()=>btn.click(),30)}window.__AKT_WATCHLIST_OPEN_DETAIL__=openDetail;async function copy(v){try{await navigator.clipboard.writeText(v)}catch(_){}}function render(){const p=document.getElementById('watchlistPanel');if(!p)return;const body=p.querySelector('.watchlist-items');body.innerHTML=list.length?list.map(x=>'<div class="watch-row"><b>'+esc(x.title||x.key)+'</b><span class="watch-wkn">WKN: '+esc(x.key)+'</span><button type="button" data-a="'+esc(x.key)+'">Analyse</button><button type="button" data-c="'+esc(x.key)+'">WKN kopieren</button><button type="button" data-r="'+esc(x.key)+'">Entfernen</button></div>').join(''):'<div class="watch-empty">Noch keine Titel markiert.</div>';p.querySelector('.watch-count').textContent='('+list.length+')';p.querySelectorAll('[data-a]').forEach(b=>b.onclick=()=>openDetail(b.dataset.a));p.querySelectorAll('[data-c]').forEach(b=>b.onclick=()=>copy(b.dataset.c));p.querySelectorAll('[data-r]').forEach(b=>b.onclick=()=>toggle({key:b.dataset.r}))}function addButtons(){document.querySelectorAll('#top10Grid .top10-item,#individuals .stock-group').forEach(el=>{const r=record(el);if(!r)return;el.dataset.watchKey=r.key;let b=el.querySelector('.watch-star');if(!b){b=document.createElement('button');b.type='button';b.className='watch-star';b.textContent=list.some(x=>x.key===r.key)?'★':'☆';(el.querySelector('summary')||el).appendChild(b)}b.onclick=e=>{e.preventDefault();e.stopPropagation();toggle(r);b.textContent=list.some(x=>x.key===r.key)?'★':'☆'}})}function init(){render();addButtons();document.querySelectorAll('[data-wl-link]').forEach(b=>b.onclick=()=>{const u=new URL(location.href);u.hash='watchlist='+enc(list);copy(u.href);alert('Watchlist-Link kopiert.')});document.querySelectorAll('[data-wl-export]').forEach(b=>b.onclick=()=>{const i=document.querySelector('[data-wl-code]');i.value=enc(list);i.select();copy(i.value)});document.querySelectorAll('[data-wl-import]').forEach(b=>b.onclick=()=>{try{const i=document.querySelector('[data-wl-code]'),v=i.value.trim();list=normalizeList(dec(v));save();i.value=''}catch(_){alert('Ungültiger Sync-Code.')}})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();