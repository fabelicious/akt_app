(function(){
  'use strict';
  const KEY='aktpro_watchlist_v1';
  const decode=v=>JSON.parse(decodeURIComponent(escape(atob(v))));
  const encode=a=>btoa(unescape(encodeURIComponent(JSON.stringify(a))));
  const load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(_){return[]}};
  let list=load();
  function esc(v){return String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
  function save(a){list=[...new Set(a.filter(Boolean))];localStorage.setItem(KEY,JSON.stringify(list));render()}
  function keyFor(el){return (el?.dataset.watchKey||el?.querySelector('.top10-wkn')?.textContent||el?.querySelector('.summary-main b')?.textContent||'').trim()}
  function toggle(key){if(!key)return;save(list.includes(key)?list.filter(x=>x!==key):[...list,key])}
  function addButtons(){document.querySelectorAll('#top10Grid .top10-item,#individuals .stock-group').forEach(el=>{if(el.querySelector('.watch-star'))return;const key=keyFor(el);if(!key)return;el.dataset.watchKey=key;const b=document.createElement('button');b.type='button';b.className='watch-star';b.title='Watchlist';b.textContent=list.includes(key)?'★':'☆';b.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();toggle(key)});const target=el.querySelector('summary')||el.querySelector('.top10-detail')||el;target.parentNode.insertBefore(b,target)})}
  function render(){const p=document.getElementById('watchlistPanel');if(!p)return;const body=p.querySelector('.watchlist-items');body.innerHTML=list.length?list.map(x=>'<span class="watch-item">'+esc(x)+'</span>').join(''):'<span class="watch-empty">Noch keine Titel markiert.</span>';p.querySelector('.watch-count').textContent='('+list.length+')';addButtons();document.querySelectorAll('.watch-star').forEach(b=>{const parent=b.closest('.top10-item,.stock-group');b.textContent=list.includes(keyFor(parent))?'★':'☆'})}
  function importList(a){if(!Array.isArray(a))throw Error('invalid');save(a.map(String));}
  function makeShareLink(){const u=new URL(location.href);u.hash='watchlist='+encode(list);return u.href}
  function initPanel(){if(document.getElementById('watchlistPanel'))return;const top=document.getElementById('top10Panel');if(!top)return;const p=document.createElement('section');p.id='watchlistPanel';p.className='watchlist-panel';p.innerHTML='<details open><summary>⭐ Meine Watchlist <span class="watch-count"></span></summary><div class="watchlist-body"><div class="watchlist-items"></div><div class="watch-sync"><button type="button" class="action-btn" data-wl-link>Link erzeugen</button><button type="button" class="action-btn" data-wl-export>Sync-Code</button><input data-wl-code placeholder="Sync-Code einfügen"><button type="button" class="action-btn" data-wl-import>Importieren</button></div><div class="watch-note">Kostenlos und dauerhaft im Browser gespeichert. Für einen anderen Browser/Gerät kann die Watchlist per Link oder Sync-Code übernommen werden. Eine vollautomatische geräteübergreifende Cloud-Synchronisation braucht ein zusätzliches Backend/Konto.</div></div></details>';top.insertAdjacentElement('afterend',p);p.querySelector('[data-wl-link]').onclick=()=>{const i=p.querySelector('[data-wl-code');i.value=makeShareLink();i.select();try{navigator.clipboard.writeText(i.value)}catch(_){}};p.querySelector('[data-wl-export]').onclick=()=>{const i=p.querySelector('[data-wl-code');i.value=encode(list);i.select()};p.querySelector('[data-wl-import]').onclick=()=>{const i=p.querySelector('[data-wl-code');try{if(i.value.startsWith('http')){const u=new URL(i.value);const h=u.hash.match(/^#watchlist=(.+)$/);if(!h)throw 0;importList(decode(h[1]))}else importList(decode(i.value.trim()));i.value=''}catch(_){alert('Ungültiger Watchlist-Link oder Sync-Code.')}};render()}
  function readHash(){try{const h=location.hash.match(/^#watchlist=(.+)$/);if(h){importList(decode(h[1]));history.replaceState(null,'',location.pathname+location.search)}}catch(_){} }
  const css=document.createElement('style');css.textContent='.watchlist-panel{margin-top:11px;background:#fff;border:1px solid #e5e7eb;border-radius:14px}.watchlist-panel summary{cursor:pointer;padding:13px 15px;font-weight:850}.watchlist-body{padding:0 15px 14px}.watchlist-items{display:flex;flex-wrap:wrap;gap:6px}.watch-item{background:#f1f5f9;border-radius:7px;padding:5px 8px;font-size:11px}.watch-empty,.watch-note{font-size:10px;color:#64748b}.watch-sync{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.watch-sync input{flex:1;min-width:180px;padding:8px;border:1px solid #cbd5e1;border-radius:7px}.watch-star{float:right;border:0;background:transparent;color:#d99b00;font-size:20px;cursor:pointer;line-height:1;padding:2px 5px;position:relative;z-index:4}.watch-note{margin-top:8px}@media(max-width:700px){.watch-sync>*{width:100%}.watch-sync input{min-width:0}}';document.head.appendChild(css);
  function boot(){readHash();initPanel();addButtons();const c=document.getElementById('individuals'),t=document.getElementById('top10Grid');if(c)new MutationObserver(addButtons).observe(c,{childList:true,subtree:true});if(t)new MutationObserver(addButtons).observe(t,{childList:true,subtree:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
