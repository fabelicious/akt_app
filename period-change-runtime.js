(function(){
'use strict';
const fmt=v=>Number(v).toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2});
function activeDays(){return Number(document.querySelector('.tab.active')?.dataset.d||180)}
function series(group){const data=group?._stock?.data; if(!Array.isArray(data))return[];return data.map(x=>{const d=x?.d instanceof Date?x.d.getTime():new Date(x?.d).getTime();const c=Number(x?.c);return{d,c}}).filter(x=>Number.isFinite(x.d)&&Number.isFinite(x.c)&&x.c>0).sort((a,b)=>a.d-b.d)}
function venue(group){return group?._stock?.dataVenue||group?._stock?.venue||group.querySelector('.venue-select option:checked')?.textContent||'aktueller Handelsplatz':'aktueller Handelsplatz'}
function update(group){const data=series(group);if(data.length<2)return false;const end=data[data.length-1],cut=end.d-activeDays()*86400000;const first=data.find(x=>x.d>=cut)||data[0];if(!first||first.c<=0)return false;const change=(end.c/first.c-1)*100,label=document.querySelector('.tab.active')?.textContent?.trim()||'6M',cls=change>0.0001?'positive':change<-0.0001?'negative':'neutral';let card=group.querySelector('.period-change-card');if(!card){const cards=[...group.querySelectorAll('.grid .card')];const course=cards.find(c=>/^kurs(?:\s|$)/i.test((c.querySelector('.label')?.textContent||'').trim()));if(!course)return false;card=document.createElement('div');card.className='card period-change-card';course.insertAdjacentElement('afterend',card)}const html='<span class="label">Veränderung '+label+'</span><div class="value '+cls+'">'+(change>0?'+':'')+fmt(change)+' %</div><div class="sub">Datenbasis: '+venue(group)+'</div>';if(card.innerHTML!==html)card.innerHTML=html;return true}
function updateAll(){let pending=false;document.querySelectorAll('#individuals .stock-group').forEach(g=>{if(!update(g))pending=true});if(pending)setTimeout(()=>document.querySelectorAll('#individuals .stock-group').forEach(update),450)}
let timer=0;function schedule(){clearTimeout(timer);timer=setTimeout(updateAll,120)}
function init(){if(window.__AKT_PERIOD_CHANGE_LOADED)return;window.__AKT_PERIOD_CHANGE_LOADED=true;const s=document.createElement('style');s.textContent='.period-change-card{padding:6px 8px!important;min-height:0}.period-change-card .label{font-size:8px}.period-change-card .value{font-size:14px;margin-top:1px}.period-change-card .sub{font-size:8px}.period-change-card .positive{color:#15803d}.period-change-card .negative{color:#b91c1c}.period-change-card .neutral{color:#64748b}';document.head.appendChild(s);const root=document.getElementById('individuals');if(root){let t=0;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(schedule,100)}).observe(root,{childList:true,subtree:true})}document.addEventListener('click',e=>{if(e.target.closest('.tab'))schedule()});document.addEventListener('change',e=>{if(e.target.closest('.venue-select'))schedule()});window.addEventListener('akt:venue-refreshed',e=>{schedule();setTimeout(schedule,350);setTimeout(schedule,900)});window.addEventListener('load',schedule);schedule();setTimeout(schedule,500);setTimeout(schedule,1500)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();