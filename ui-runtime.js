(function(){'use strict';
function openClose(){const all=()=>[...document.querySelectorAll('#individuals .stock-group')];const open=document.getElementById('openAll'),close=document.getElementById('closeAll');if(open)open.onclick=e=>{e.preventDefault();e.stopImmediatePropagation();all().forEach(g=>g.open=true)};if(close)close.onclick=e=>{e.preventDefault();e.stopImmediatePropagation();all().forEach(g=>g.open=false)}}
function recommendationMeter(root){root.querySelectorAll('.stock-group .card').forEach(card=>{const text=card.textContent||'';if(!/Technische Empfehlung/i.test(text)||card.querySelector('.tech-recommendation-meter'))return;const scoreEl=card.querySelector('.score,.value');let score=parseFloat((scoreEl?.textContent||'').replace(',','.'));if(!Number.isFinite(score))return;score=Math.max(0,Math.min(100,score));const meter=document.createElement('div');meter.className='tech-recommendation-meter';meter.innerHTML='<div class="meter"><span></span></div>';meter.querySelector('span').style.width=score+'%';card.appendChild(meter)})}
function init(){openClose();const root=document.getElementById('individuals');if(root){let t;const run=()=>{clearTimeout(t);t=setTimeout(()=>recommendationMeter(root),100)};new MutationObserver(run).observe(root,{childList:true,subtree:true});run()}const css=document.createElement('style');css.textContent='.tech-recommendation-meter{margin-top:7px;width:100%}.tech-recommendation-meter .meter{width:100%;height:7px;background:#e5e7eb;border-radius:9px;overflow:hidden}.tech-recommendation-meter .meter span{display:block;height:100%;background:#38bdf8;border-radius:9px}';document.head.appendChild(css)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();
