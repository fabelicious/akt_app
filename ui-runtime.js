(function(){'use strict';
function groups(){const r=document.getElementById('individuals');return r?[...r.querySelectorAll('details.stock-group')]:[]}
function bind(){if(document.documentElement.dataset.ocBound)return;document.documentElement.dataset.ocBound='1';document.addEventListener('click',e=>{const b=e.target.closest&&e.target.closest('#openAll,#closeAll');if(!b)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const open=b.id==='openAll';groups().forEach(g=>g.open=open)},true)}
function arrange(g){const cards=[...g.querySelectorAll('.card')];const rec=cards.find(c=>(c.querySelector('.label')?.textContent||'').trim()==='Technische Empfehlung');const per=cards.find(c=>c.classList.contains('period-change-card')||c.classList.contains('period-side-card')||/Veränderung/i.test(c.querySelector('.label')?.textContent||''));if(rec&&per){let row=g.querySelector('.recommendation-row');if(!row){row=document.createElement('div');row.className='recommendation-row';rec.parentNode.insertBefore(row,rec)}row.append(rec,per)}const course=cards.find(c=>(c.querySelector('.label')?.textContent||'').trim()==='Kurs');if(course){course.style.height='auto';course.style.maxHeight='none';course.style.overflow='visible';const s=course.querySelector('.venue-select');if(s){s.hidden=false;s.style.display='block';s.style.visibility='visible';s.style.opacity='1'}}}
function init(){bind();const root=document.getElementById('individuals');if(!root)return;let t=0;const run=()=>{clearTimeout(t);t=setTimeout(()=>groups().forEach(arrange),80)};new MutationObserver(run).observe(root,{childList:true,subtree:true});run()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();